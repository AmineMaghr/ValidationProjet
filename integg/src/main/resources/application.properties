# Midgar Application Configuration (MySQL)
# Generated/Updated: $(date)

# Application
app.name=Midgar
app.version=1.0.0
app.title=Plateforme de Création Fantasy

# UI
ui.width=1200
ui.height=800
ui.theme=dark

# Database (MySQL)
db.type=MySQL
db.url=jdbc:mysql://localhost:3306/midgar37?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=
db.persistent=true

# AI API (Grok)
ai.grok.api_url=https://api.x.ai/v1/chat/completions
ai.grok.model=grok-beta
ai.grok.api_key=

# Debug
debug.enabled=true
debug.verbose=false

# Notes:
# 1. Start MySQL server
# 2. CREATE DATABASE midgar37;
# 3. mysql midgar37 < init_database_midgar37.sql
# 4. Update db.password if needed
# 5. mvn javafx:run

